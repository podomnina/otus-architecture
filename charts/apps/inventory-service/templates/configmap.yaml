apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap
data:
  config.yml: |

    ru:
      otus:
    
    webclient:
      menuService:
        url: "http://menu-service:8080"